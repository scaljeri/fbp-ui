import { InteractionService } from 'src/app/services/interaction.service';

export interface IFbpInteractionSocketContext {

}

export const interaction2SocketContext = (service: InteractionService): IFbpInteractionSocketContext => {
	return { };
}